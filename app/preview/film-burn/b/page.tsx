import { PreviewShell } from '@/components/site/preview-shell'

export default function B() {
  return (
    <PreviewShell
      step="two"
      href="/preview/film-burn"
      accent="#ffd9a0"
      bg="linear-gradient(150deg, #33235c, #4a2b7a 55%, #2a1c4d)"
    >
      And back — the fire starts from a fresh corner each time.
    </PreviewShell>
  )
}
