import { PreviewShell } from '@/components/site/preview-shell'

export default function B() {
  return (
    <PreviewShell step="two" href="/preview/spaces" accent="#5d7280" bg="#c6d5de" fg="#151a1e">
      And back — it knows this is the way home, so everything slides right.
    </PreviewShell>
  )
}
