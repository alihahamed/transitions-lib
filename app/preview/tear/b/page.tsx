import { PreviewShell } from '@/components/site/preview-shell'

export default function B() {
  return (
    <PreviewShell step="two" href="/preview/tear" accent="#54636d" bg="#1e262c" fg="#dfe7ec">
      And back — the seam is seeded from the page you leave, so it tears the same way each time.
    </PreviewShell>
  )
}
