import { PreviewShell } from '@/components/site/preview-shell'

export default function B() {
  return (
    <PreviewShell step="two" href="/preview/zipper" accent="#8f887c">
      And back — watch it catch twice on the way down, then run.
    </PreviewShell>
  )
}
