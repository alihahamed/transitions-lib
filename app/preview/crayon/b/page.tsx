import { PreviewShell } from '@/components/site/preview-shell'

export default function B() {
  return (
    <PreviewShell step="two" href="/preview/crayon" accent="#0066ff">
      And back again — watch the stagger, each stroke leaves on its own beat.
    </PreviewShell>
  )
}
